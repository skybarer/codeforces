#include <algorithm>
#include <iostream>
using namespace std;

int main() {
    int testCases;
    cin >> testCases;

    while (testCases--) {
        long long a, b, c, d;
        cin >> a >> b >> c >> d;

        cout << b << " " << c << " " << c << endl;
    }
}